<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Rumbl · Phoenix Framework</title>
  <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>" />
  <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
</head>

<body class="bg-cool-gray-100">
  <header>
    <section class="container p-4 mx-auto bg-cool-gray-100">
      <nav role="navgation">
        <ul class="flex">
          <%= if @current_user do %>
            <li class="mr-6 bg-teal-50 p-4 text-teal-600 font-mono">
              Welcome back <%= @current_user.username %>
            </li>
            <li class="mr-6 bg-teal-100 p-4 text-teal-700 hover:bg-teal-300">
              <%= link "My Videos", to: Routes.video_path(@conn, :index) %>
            </li>
            <li class="mr-6 bg-teal-100 p-4 text-teal-700 hover:bg-teal-300">
              <%= link "Log out", to: Routes.session_path(@conn, :delete, @current_user), method: "delete" %>
            </li>
          <% else %>
            <li class="mr-6 bg-teal-100 p-4 text-teal-700 hover:bg-teal-300"><%= link "Register", to: Routes.user_path(@conn, :new) %></li>
            <li class="mr-6 bg-teal-100 p-4 text-teal-700 hover:bg-teal-300"><%= link "Log in", to: Routes.session_path(@conn, :new) %></li>
          <% end %>
        </ul>
      </nav>
    </section>

  </header>

  <main role="main" class="container p-4 mx-auto">
    <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
    <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
    <%= @inner_content %>
  </main>

  <script>
    window.userToken = "<%= assigns[:user_token] %>"
  </script>
  <script type="text/javascript">
    src = "<%= Routes.static_url(@conn, "/js/app.js") %>"
  </script>

</body>

</html>
